name: CI

on:
  pull_request:
  push:
    branches: [main, master]

jobs:
  check:
    name: check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # {{STACK_SETUP}}

      - name: Run full regression suite
        run: ./check.sh
