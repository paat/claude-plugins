diff --git a/src/webhook.ts b/src/webhook.ts
index 1111111..2222222 100644
--- a/src/webhook.ts
+++ b/src/webhook.ts
@@ -8,9 +8,7 @@ export function handle(req, res) {
   const sig = verify(req);
-  if (!sig.valid) {
-    res.status(401).json({ error: "invalid signature" });
-    return;
-  }
   process(req.body);
   res.status(200).json({ ok: true });
 }
