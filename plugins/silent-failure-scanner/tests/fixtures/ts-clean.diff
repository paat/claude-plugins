diff --git a/src/payment.ts b/src/payment.ts
index 1111111..2222222 100644
--- a/src/payment.ts
+++ b/src/payment.ts
@@ -10,6 +10,9 @@ export async function charge(order: Order) {
   try {
     await db.persist(order);
+    await audit.log(order.id);
   } catch (err) {
     logger.error("persist failed", err);
     throw err;
   }
 }
