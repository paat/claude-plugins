{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'p=hooks/check-source-edit.sh; for r in \"${CLAUDE_PLUGIN_ROOT:-}\" \"${CODEX_PLUGIN_ROOT:-}\" \"$PWD\" \"$PWD/..\"; do [ -n \"$r\" ] && [ -f \"$r/$p\" ] && exec bash \"$r/$p\"; done; cat >/dev/null 2>&1 || true; echo \"[agent-sync] hook target not found: $p\" >&2; exit 0'",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
