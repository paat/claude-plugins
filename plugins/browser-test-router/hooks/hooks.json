{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check if the Bash command is browser-related (curl, wget, playwright). If yes, output: '[browser-test-router] Consider delegating to Kimi K2.5 via opencode run -m opencode/kimi-k2.5-free for token savings and superior tool handling. Run /browser-test-router:browser-test for full delegation workflow.' If not browser-related, output nothing."
          }
        ]
      }
    ]
  }
}
